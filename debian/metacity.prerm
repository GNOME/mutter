#!/bin/sh

set -e

case "$1" in
  remove)
    update-alternatives --remove x-window-manager \
        /usr/bin/metacity
  ;;
  failed-upgrade)
  ;;
  *)
    echo "prerm called with unknown argument \`$1'" >&2
    exit 1
  ;;
esac

#DEBHELPER#

