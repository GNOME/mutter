image: registry.gitlab.gnome.org/GNOME/mutter/master:v1

stages:
 - build

build-mutter:
  stage: build
  script:
    - env NOCONFIGURE=1 ./autogen.sh
    - mkdir build
    - cd build
    - ../configure --enable-egl-device --enable-debug --enable-egl --enable-unit-tests
    - make -j9
