<!DOCTYPE node PUBLIC
'-//freedesktop//DTD D-BUS Object Introspection 1.0//EN'
'http://www.freedesktop.org/standards/dbus/1.0/introspect.dtd'>
<node>

  <!--
      org.gnome.Mutter.WindowList:
      @short_description: Window list interface

      This API is private and not intended to be used outside of the integrated
      system that uses libmutter. No compatibility between versions are
      promised.
  -->
  <interface name="org.gnome.Mutter.WindowList">

    <!--
        GetList:

        Retrieves the current list of windows and their properties

	A window is exposed as:
        * t ID: unique ID of the window
        * a{sv} properties: high-level properties

          Known properties:

          - "title"      (s): (readonly) title of the window
          - "class"      (s): (readonly) class/application ID of the window
          - "type"       (u): (readonly) 0 for Wayland, 1 for X11
          - "is_visible" (b): (readonly) if the window is currently
                                         visible on screen
          - "has_focus"  (b): (readonly) if the window currently have
                                         keyboard focus
          - "width"      (u): (readonly) width of the window
          - "height"     (u): (readonly) height of the window
          - "pid"        (t): (readonly) process ID of the client owning
                                         the window
    -->
    <method name="GetList">
      <arg name="windows" direction="out" type="a(ta{sv})" />
    </method>

  </interface>

</node>
